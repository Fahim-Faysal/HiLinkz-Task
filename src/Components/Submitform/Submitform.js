import React from 'react';

const Submitform = ({ from }) => {
      return (
            <div>
                  <form action="">

                  </form>
            </div>
      );
};

export default Submitform;